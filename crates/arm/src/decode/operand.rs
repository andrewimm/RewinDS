//! Shared helpers for decoding operand fields (shifts, immediates, register
//! lists) reused across instruction classes.
//!
//! Empty for now; helpers land here as the per-class decoders are implemented.
