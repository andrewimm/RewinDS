[package]
name = "gba"
version = "0.1.0"
edition = "2021"
description = "GBA hardware semantics"

[dependencies]
video2d = { path = "../video2d" }
arm = { path = "../arm" }
emu-core = { path = "../emu-core" }
