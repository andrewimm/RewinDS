[package]
name = "rewinds"
version = "0.1.0"
edition = "2021"
description = "RewinDS emulator — desktop runner"

[dependencies]
emulator = { path = "../emulator" }
debug = { path = "../debug" }
minifb = "0.27"
cpal = "0.15"
log = "0.4"

[dev-dependencies]
arm = { path = "../arm" }
nds = { path = "../nds" }
