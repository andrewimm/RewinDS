[workspace]
resolver = "2"
members = ["crates/arm", "crates/emu-core", "crates/gba", "crates/debug"]

# Other crates (nds, video2d, jit) will be added to the workspace as they gain
# source and manifests.
